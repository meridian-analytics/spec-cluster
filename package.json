{
  "name": "spec-cluster-repo",
  "private": true,
  "scripts": {
    "check": "bun --filter '*' check",
    "format": "bun --filter '*' format",
    "lint": "bun --filter '*' lint",
    "tsc": "bun --filter '*' tsc"
  },
  "workspaces": ["packages/*"]
}
