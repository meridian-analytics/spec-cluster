{
  "name": "spec-cluster-repo",
  "private": true,
  "dependencies": {
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.0",
    "@types/three": "^0.164.0"
  },
  "devDependencies": {
    "@biomejs/biome": "1.6.1"
  },
  "scripts": {
    "check": "biome check --apply .",
    "format": "biome format --write .",
    "lint": "biome lint --apply .",
    "tsc": "bun --filter '*' tsc"
  },
  "workspaces": ["packages/*"]
}
